pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout your source code from your version control system (e.g., Git)
                // For Git:
                git 'https://github.com/shivaayrawat/sonartest.git'
            }
        }

        stage('SonarQube Analysis') {
                 environment { Sonarurl= "http://20.219.236.142:9010"}
            steps {
                withCredentials([string(credentialsId: 'sonarqube', Variable: 'sonartest')]) {
                    sh 'mvn sonar:sonar -Dsonar.login=$testing for demo -Donar.host.url=${Sonarurl}'
            }
        }
    }
 }
}


